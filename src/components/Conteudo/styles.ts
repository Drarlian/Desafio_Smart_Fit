import styled from "styled-components";

export const DivConteudo = styled.div`
    /* background-color: pink; */

    display: flex;
    flex-direction: column;
    flex: 1;  // Preenchendo o espaço vazio.
    padding: 0;
    margin: 0;
`;